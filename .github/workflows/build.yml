name: Build

on:
  release:
    types: [created]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-windows:
    runs-on: windows-2025
    defaults:
      run:
        shell: pwsh

    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-go@v6
        with:
          go-version: '1.22'
          cache-dependency-path: windows/go.sum

      - name: Tidy and download dependencies
        run: |
          cd windows
          go mod tidy
          go mod download

      - name: Test
        run: |
          cd windows
          go test ./...

      - name: Build
        env:
          CGO_ENABLED: 0
        run: |
          cd windows
          New-Item -ItemType Directory -Force -Path ..\dist | Out-Null
          go build -ldflags "-H windowsgui -s -w" -o ..\dist\Hotfix.exe .

      - name: Upload artifact (all runs)
        uses: actions/upload-artifact@v7
        with:
          name: Hotfix-Windows
          path: dist/Hotfix.exe

      - name: Upload to release
        if: github.event_name == 'release'
        uses: softprops/action-gh-release@v3
        with:
          files: dist/Hotfix.exe

  build-macos:
    runs-on: macos-latest

    steps:
      - uses: actions/checkout@v6

      - name: Install librsvg (for icon generation)
        run: brew install librsvg

      - name: Test
        run: swift test

      - name: Build DMG
        run: bash scripts/build.sh

      - name: Upload artifact (all runs)
        uses: actions/upload-artifact@v7
        with:
          name: Hotfix-macOS
          path: dist/Hotfix.dmg

      - name: Upload to release
        if: github.event_name == 'release'
        uses: softprops/action-gh-release@v3
        with:
          files: dist/Hotfix.dmg
